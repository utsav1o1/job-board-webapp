<input {{ $attributes->merge(['type' => 'text']) }} placeholder="{{ $placeholder }}" value="{{ $value }}" name="{{ $name }}"
    id="{{ $name }}"
    class="w-full border-0 rounded-md py-1.5 px-2.5 text-sm ring-1 ring-slate-300  placeholder:text-slate-400 focus:ring-2" />
